import type { Plugin, ResolvedConfig } from 'vite';
import type { Options } from './typings';
export declare function cleanupCache(deps: string[], config: ResolvedConfig): Promise<void>;
export default function rollback(opts: Options): Plugin;
export * from './typings';
