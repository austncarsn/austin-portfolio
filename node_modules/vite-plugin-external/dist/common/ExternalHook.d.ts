import { ExternalFn } from '../typings';
export default class ExternalHook {
    readonly hooks: ExternalFn[];
    use(arg: ExternalFn | boolean | string | RegExp | Array<string | RegExp> | Record<string, string>): this;
}
