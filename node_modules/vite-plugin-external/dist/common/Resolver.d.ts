import type { ExternalES, ExternalFn, ExternalIIFE } from '../typings';
export declare function stash(libName: string, globalName: string, cacheDir: string): Promise<ExternalIIFE | ExternalES>;
export declare class Resolver {
    private readonly cacheDir;
    readonly stashMap: Map<string, ExternalIIFE | ExternalES>;
    private readonly resolveHooks;
    constructor(cacheDir: string);
    stash(libName: string, globalName: string): Promise<ExternalIIFE | ExternalES>;
    resolve(source: string, importer: string | undefined, isResolved: boolean): Promise<ExternalIIFE | ExternalES | boolean>;
    useHook(hook: ExternalFn): this;
}
