import { ILayout, Level, ConsoleAppender, LogEvent } from 'base-log-factory';
export interface ColorfulAppenderOptions {
    layout?: ILayout;
    colors?: Record<Level, string>;
}
/**
 * Console appender (控制台输出)
 */
export declare class ColorfulAppender extends ConsoleAppender {
    name: string;
    colors: Record<Level, string>;
    /**
     * Constructor (构造函数)
     */
    constructor(opts?: ColorfulAppenderOptions);
    /**
     * Write log (写入日志)
     * @param event Log event (日志事件)
     */
    write(event: LogEvent): void;
    protected color(str: string, event: LogEvent): string;
}
