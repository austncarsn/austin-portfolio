"use strict";
const addObject = require("./addObject-BB40bW2H.js");
function add(date, num, unit) {
  return typeof num === "object" && num !== null ? addObject.addObject(date, num, 1) : addObject.addNumber(date, num, unit);
}
module.exports = add;
