import { a as absFloor } from "./absFloor-BlD_nfEw.mjs";
import { c as MS_IN_SECOND } from "./constants-BuFquuHl.mjs";
import parse from "./parse.mjs";
function diffInSeconds(date, input, asFloat) {
  const parsed = parse(input);
  const output = (+date - +parsed) / MS_IN_SECOND;
  return asFloat ? output : absFloor(output);
}
export {
  diffInSeconds as default
};
