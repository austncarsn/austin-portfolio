import { a as addObject, b as addNumber } from "./addObject-CJjLNrjF.mjs";
function subtract(date, num, unit) {
  return typeof num === "object" && num !== null ? addObject(date, num, -1) : addNumber(date, -num, unit);
}
export {
  subtract as default
};
