"use strict";
const constants = require("./constants-DR3SPJ7u.js");
const units = constants.rangeOfUnits.reduce((acc, unit) => {
  acc[unit.toUpperCase()] = unit;
  return acc;
}, {});
module.exports = units;
